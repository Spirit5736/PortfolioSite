import { ByPassSecurityPipePipe } from './by-pass-security-pipe.pipe';

describe('ByPassSecurityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ByPassSecurityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
